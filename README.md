# 🔥 azufw

A modern Terminal UI (TUI) for UFW firewall management.

## Features

- View all firewall rules in a clean table
- Colored action badges (ALLOW/DENY/LIMIT)
- Firewall status indicator
- Keyboard navigation
- Lightweight and fast

## Installation

```bash
pip install azufw
```
